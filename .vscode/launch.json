{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Open index.html with Live Server",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}